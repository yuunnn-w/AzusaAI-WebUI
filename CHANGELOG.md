# 更新日志

本项目所有值得记录的变更都写在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.2] - 2026-09-20

### 新增

- `rust-proxy` 的「关于」换成一整套使用教程：8 章，含所有命令行参数、常见故障与排查步骤，窗口内可滚动查阅。
- 主面板新增**实时网络流量曲线**：上下行速率各一条线、单位自适应（`B/s` → `KB/s` → `MB/s`），替代原来的延迟波动图。

### 变更

- 面板读数统一到最近 120 秒的时间窗：请求 / 活跃 / 预检 / 错误 / 延迟都按窗内实测算，不再出现「早已停用、面板还停在旧值」；累计字节另标「自启动累计」。
- 设置窗改为列栅格排布，字段不再互相挤压；「高级选项（剥离前缀）」回到它该在的列。
- 点「应用」后有明确进度到落定：不再长时间停在「正在应用到本次运行…」，也不会出现「超时」字样。
- 日志窗限 2000 条并可滚动、可整段复制；新行逐行追加，不再出现爬字与残影。
- 主程序（三档单文件产物）本版无功能改动，仅同步版本号与文档。

### 修复

- 修设置窗在小尺寸 / 高 DPI 下底部被裁掉，以及相邻字段错位到同一行的问题。
- 修窄窗口下曲线轴标签整排消失：Y 轴刻度改为恒画，空间不够才逐档降级。
- 修日志窗重影：滚动过视图后旧像素残留，新行会叠在旧内容上。
- 修客户端主动断开被误记成上游错误（错误计数虚高，复现 39 → 0）。
- 修「停止」时终态行偶尔不落，以及没点过「应用」就点「停止」时的假报错。
- 教程正文里外露的 `**` 标记去掉，全文按正常文字排版。

### 已知限制

- 面板与曲线只反映最近 120 秒的窗口视图，重启即清零（程序不写任何文件，历史不落盘）。
- Win7 实机冒烟仍未做：本版只在 Win10 / Win11 上验证过。

## [0.2.1] - 2026-09-19

### 新增

- 多会话并发：多条会话可以同时生成，会话列表显示各自状态；上限在设置里调（默认 3），超出的消息排队后自动补发。
- `ReadOffice` 读 Excel 默认给「地址 → 值」的 grid 视图；想退回旧的 CSV 观感就传 `sheet_view:"csv"`。
- `Office` 能写 Excel（xlsx）：新建工作簿、改单元格、增删改工作表、插删整行，只改你点名的部分；`.xls`（旧格式）也能读、能按值改写、能转成 `.xlsx`。
- docx / pptx 补上段落、表格与形状级编辑：改整段文字、改表格单元格、删形状、改位置尺寸、加文本框、移动幻灯片。
- docx 的「图片」档改为逐页渲染成页图（内嵌渲染器，离线可用），失败会如实回退并说明原因。
- xlsx / xls 的「图片」档换成本机自绘的表格图，只还原数据网格。
- 「混合」档下文字与文档里的图片按位置交错发送；一次处理的图片上限 6 → 10 张。
- 新子项目 `rust-proxy/`：Win7 兼容的本地反向代理，解决用 `file://` 打开时内网模型服务的 CORS 死路；发布附件 `azusa-local-proxy-win7-x64.exe`。
- `rust-proxy` 关闭按钮进托盘、最小化进任务栏，两个状态都能从托盘图标唤回窗口。
- `rust-proxy` 改走命令行参数：不写配置文件、日志只在内存（可复制到剪贴板）、不写注册表、也没有开机自启。
- `rust-proxy` 分开源版与内网版两个 exe：开源版默认上游 `http://127.0.0.1:8080`，内网版在构建时注入地址、本地交付。

### 变更

- 「PDF 处理方式」改名「附件图片策略」：PDF 与办公文档共用一档，恒显三档（纯文本 / 图片 / 混合），默认由「纯文本」改为「混合」；想省 token 就选「纯文本」。
- 迁移：附件图片策略的设置键由 `pdfMode` 改为 `attachImgMode`（三值：纯文本 / 图片 / 混合），旧 `pdfMode` 只保留「显式选过图片」这一种，其余（含从没设置过）一律落到「混合」；旧键原样保留，回退旧版本仍能读到。
- 选「纯文本」后附件不再附带任何图片内容；没有文字层的扫描件照旧自动升档并走本机 OCR。
- 设置项「停止生成时也终止后台任务」移除：停止生成不再中止后台 Python 任务，任务会跑完并回写工作区，中止任务只剩 `/tasks` 一个入口。
- 多会话并发后，新建会话、给别的会话发消息 / 清空 / 编辑 / 重新生成等都不再被「生成进行中」挡住。
- 不支持图像输入的模型：文档里的图片自动本机 OCR 后按位置并入正文。
- Jupyter 标签页底部的日志栏删除；设置面板的附件说明与环境面板引擎行同步更新。

### 修复

- 修好并发带来的一批串扰：切走再切回后流式气泡不刷新、用量与「停止」记到别的会话、后台任务续跑发错会话等。

### 已知限制

- pptx 的「图片」档本版不做，选它会如实降级并在气泡里说明；docx 页图按浏览器排版分页，与 Word 的自动分页可能不一致。
- pptx 删页后页号会跳号：`ReadOffice` 显示的页号与 `Office` 的写坐标不是一回事，写请以 `outline` 的序号为准。
- xlsx 的插入 / 删除行不重写公式引用，改过结构后请自行复核相关公式。
- `.xls` 是值级通道：公式、样式、图表、批注、文档属性都不保留（每次都会逐条提示）；单文件上限 1.5 MB / 15 万格。
- Word / PowerPoint 的 COM 校验未做：`move_slide` 与 `set_paragraph` 在真 Office 里的观感尚未实测。
- `rust-proxy` 未做 Win7 实机冒烟，在完成实机验证前不要把它标为「已在 Win7 验证」。

## [0.2.0] - 2026-09-18

### 新增

- 新增 `DownloadForUser` 工具：把工作区的文件或目录（自动打包 zip）下载到本机，默认权限「询问」，单次上限 128 MB。
- 新增 `Office` 工具：在工作区里生成与改写 docx / pptx，默认权限「禁止」，要用请在 设置 → MCP 工具 里放开。
- pptx 的抽取文本带上页标记，`ReadOffice` 可按页读 pptx；非视觉模型下幻灯里的图片经本机 OCR 后插到对应幻灯段之后。
- 工作区侧栏支持右键菜单（重命名 / 删除 / 剪切 / 复制 / 粘贴 / 下载 / 新建等）与 Ctrl / Shift 多选批量操作，批量失败逐条列出。
- 设置 → 外观 新增「桌面快捷方式」：把页面图标生成为 Windows 快捷方式与多尺寸 `.ico`，可下载或直接写入文件夹。
- JupyterLite 标签页：右键「通过 JupyterLab 打开」会新起一个完整 JupyterLab 4.6.3，以工作区为根、跑同一份内嵌 Python，与主页面 1 秒节拍双向同步，离线可用。
- 提示词模板从 8 条扩到 18 条 6 组（办公 / 写作 / 科研 / 数据 / 代码 / 会议）；你改过的模板库不会被覆盖。

### 变更

- 单文件下载、目录打包与「整箱 zip」合并成同一实现，「整箱」改为逐文件懒加载；下载与上传进行中会互相拒绝。
- 命令面板（`Ctrl+K`）重排：默认只列 13 条动作，模型与对话要输入关键词才出现并支持搜消息正文；`↑↓` 首尾循环、`Tab` 移动选中项。
- 命令面板删了三样：MCP 逐工具条目（入口统一到设置）、两条「清空」合并为「清空本对话消息」、快捷键重复显示。
- 上下文压缩改为工具气泡原位 + 实时流式：过程逐字可见，失败会写明「原文完整保留」并给重试，`Ctrl+X` 可随时取消。
- Jupyter 同步修好一处时间戳比较错误（曾把刚写的新内容静默还原成旧副本）；冲突改为可见提示，已打开且没有未保存修改的文档会跟随更新。
- 浮窗定位改用布局盒计量、首次打开即稳定，打开时滚动位置归零；一批按钮的可点区域补到至少 24px（右栏行高因此 +2px）。
- 聊天区助手头像改为品牌小猫，顶栏「打开工作区侧栏」按钮移到最右侧；帮助面板快捷键表补上 `F1 / ?`。
- 「整箱 zip」不可用时不再建议改用单文件下载（同一项能力），改为指出「导出备份」这条出路。

### 修复

- 生成参数浮窗与上下文浮窗底部的动作行曾被自身高度上限裁掉（不可见、不可点），已修复。
- Jupyter 同步状态行一条持续数秒的假错误（把内核模型误报成「连接形状不符」）已修复。
- 压缩摘要请求失败曾被误判为致命错误、不走重试链，已修复。

### 已知限制

- Jupyter 标签页依赖主页面：标签页地址栏是 `about:blank`，刷新会白屏；重新从工作区打开即可恢复，不丢数据。

## [0.1.1] - 2026-09-15

### 新增

- `ReadOffice`：办公六格式与 PDF 的阅读从 `Read` 拆出，`Read` 遇到这类文件改为给出指路的错误。
- `ReadOffice` 支持按页 / 表 / 幻灯与按全局行号两套坐标并互相换算，截断时给出可直接照抄的续读参数。
- 上下文压缩：接近窗口上限时提醒用量，支持 `/compact`、`Compact` 工具与设置面板三个入口，先生成交接总结再删除较早的消息与附件。
- 自动压缩：按阈值（默认 85%，可调或关闭）自动压缩；被服务端以「上下文过长」拒绝时自动压缩后重跑该轮，被丢弃消息的附件立即释放。
- 斜杠命令 `/compact`、`/clear`、`/help`、`/tasks`；`/tasks` 面板列出当前会话的后台 Python 任务与实时输出，可停止任务。
- 关于弹窗重构（七张教程卡 + 斜杠命令表 + 快捷键表）；工作区文件可就地预览，目录可就地展开浏览。
- 生成参数浮层与设置里可手动设定上下文长度（8K–1M）；`AskUser` 提问工具让模型在对话里向你提问（可选可跳）。
- `Read` 支持图片（交给支持视觉的模型，不支持时自动本机 OCR 只回文本）；非视觉模型读文档时，文档里的图片按顺序 OCR 后插进正文。
- 工作区路径口径提示：写错方向时给出可读纠错（工作区工具用 `/…`，Python 里同一份数据在 `/workspace`）。
- `Read` 的图片在工具卡片里显示缩略图；「优化存储」把工具读取的图片也纳入清理范围。
- CORS 失败分层诊断：诊断面板给出四条探针的矩阵与结论，并附服务端要补的响应头。
- 新增「预检规避兼容模式」（默认关闭）：请求改走简单请求绕过 OPTIONS 预检，仅「OpenAI 兼容 + 未填 API Key」时可用。

### 变更

- 数据面板的「80 条裁剪」改名「精简历史（只留最近 80 条）」，与「压缩上下文」区分开。
- `/clear` 复用「清空消息」入口，补上忙碌门与附件即时释放，确认弹窗写明后台任务与工作区文件不受影响。
- 压缩摘要改为携带会话的全部历史（此前看不到中段历史），输出预算改为 `min(窗口, 128K)` 并接入重试链；压缩可用 `Ctrl+X` 随时取消，失败提示细分为十类。
- 中断键改为 `Ctrl+X`（macOS `Cmd+X`）；普通 `Esc` 只负责关浮层并丢弃待发送内容。
- 生成中的输入不再被吞掉：消息进入「待发送」缓冲区，在下一个工具轮边界插入会话（支持中途补充需求），一轮结束后兜底发送，可编辑或丢弃。
- `ExecutePython` 去掉 `packages` 参数，全部预置包在首次执行前自动装载（`import` 即用），装载时间不计入超时；解释器空闲回收从 5 分钟放宽到 10 分钟。
- 紧凑模式优化：顶栏、行距、头像、输入区一并收紧并关闭毛玻璃，更窄的窗口也能三栏共存。
- `AskUser` 单题单选时点选项即自动提交；工具读取的图片不再无脑随请求重发（超过最近 8 条 / 8 MiB 的不再带走）。
- 命令面板新增「压缩上下文」「清除本会话上下文」「后台任务」入口；「快捷键与关于」改名「使用教程与关于」。
- 「申请持久化存储」提示改为信息级说明（Chrome 106 起由浏览器静默决策，「未授予」是正常现象）。

### 修复

- `Read` 不再把中文等合法文本误判成二进制，改为按编码自动识别并按行读出；二进制一律拒绝并报出类型。
- `Edit` 拒绝非 UTF-8 与含 NUL 的文件，纯 CRLF 文件往返读写不再失配。
- `Grep` 不再跳过中文文件、可指向单个文件；`Glob` 新增 `head_limit`；超长单行支持续读。
- 非 UTF-8 文本附件按真实编码读取并注明转码（此前静默乱码）；二进制改名 `.txt` 仍被拒收。
- 非流式响应里同一条消息的多个工具调用不再被合并；启动后的第一笔保存不再覆盖窗口期内的内存改动。
- 后台任务之后的续思写进它自己的思考块，任务卡片日志可正常拖拽；`PythonPackages` 改为分页，`TaskOutput` 的输出截断补上注记。
- `AskUser` 卡片的滚动、多题轮播与收起体验修复；右栏拖拽上传不再误报「没有可上传的文件」。
- 右栏在窄窗口下打不开、缩窄收起后拉宽不恢复的问题修复；外部 MCP 工具列表改为限高滚动；弹窗按钮宽度统一。
- 「估算的下一轮用量」补齐两块遗漏的系统注入；工具结果的「完整输出可用 `TaskOutput` 取」提示只在真的被截断时出现。

### 已知限制

- 自动压缩会产生一次额外模型请求，并且是不可逆的消息删除；可在设置里关闭或调阈值，手动压缩必带确认弹窗。
- 摘要请求的输出预算不受会话 `max_tokens` 影响、消耗不计入会话统计；长会话首次尝试可能要数分钟，可随时取消。
- 首次执行 Python 会先装载本档全部预置包（完整版 151 / 正常版 99 / 极简版 33），界面有进度、不计入超时。
- 工具 schema 变化会让远端为相关对话重新 prefill 一次（首字变慢，历史内容不受影响）。

## [0.1.0] - 2026-09-14

首个公开版本：单文件、离线、零外部请求的对话终端，内置 Python 运行时与模型侧工具。

### 新增

- 双协议（OpenAI 兼容 / Anthropic）一键切换；流式逐字输出，不支持时降级为非流式。
- 消息与思考过程都渲染 Markdown（表格 / 任务列表 / 引用 / 围栏代码）与 KaTeX 公式，思考过程可折叠并显示耗时。
- 代码块表头显示语言与行数，超 10 行块内滚动、可展开可复制；HTML / SVG 可一键运行。
- 重新生成走分支、不删后续消息；编辑任意用户消息后重新生成会自动截断其后的消息。
- 上下文用量环实时显示用量与构成；token 优先取服务端 `usage`，速度取客户端实测吞吐。
- 会话管理：新建 / 重命名 / 置顶 / 副本 / 删除 / 分组 / 搜索 / 会话内查找 / 命令面板。
- 附件支持纯文本 / 图片 / PDF / 办公文档，单文件 ≤ 50MB、一条消息 ≤ 10 个、总量默认 ≤ 100MB；二进制拒收并报出类型。
- 图片附件支持 PNG / JPG / GIF / WebP / BMP / AVIF / SVG / ICO / TIFF，超「图片最大边长」自动下采样。
- 非视觉模型下图片与文档里的图走内嵌 Tesseract.js OCR；PDF 可抽文字或逐页渲染成图片。
- 办公六格式（doc / docx / ppt / pptx / xls / xlsx）本机解析；附件卡片、预览弹窗与图片灯箱；语音输入与朗读。
- 内嵌 pyodide 314.0.6（CPython 3.14.2）+ 预置包 wheel，离线可跑 Python；三档分别为 151 / 99 / 33 个包。
- `ExecutePython` 支持代码或工作区脚本、参数 / stdin / 超时，输出与退出码一并回传。
- Python 任务管理：前台 / 后台 + `TaskList` / `TaskOutput` / `WaitFor` / `TaskStop`；`PythonPackages` 查包。
- 会话工作区（沙箱虚拟盘）：6 个文件工具、上传 / 下载 / 整箱 ZIP、挂载与持久化存储申请。
- 模型侧 MCP 工具框架：权限三态（允许 / 询问 / 禁止）+ 限额；内置 `ExecuteJavaScript` 沙箱（独立 Worker、默认无网络）。
- 工具调用与正文渲染在同一气泡，过程是可展开的工具卡片；可接入外部 MCP 服务器。
- 12 种马卡龙主题色 + 深色 / 浅色 / 跟随系统 + 紧凑模式；设置面板 7 个标签页与环境自检面板。
- 会话与附件存 IndexedDB，localStorage 只放设置；打开时自动整理（旧库搬家 + 回收无引用附件），无损、不做无感删除。
- 「清理空间」需勾选 + 确认才动数据，带保护范围滑块；导出 / 导入备份可完整恢复；多标签页同步；提示词模板库。

### 变更

- 应用更名为 AzusaAI WebUI；存储键与 IndexedDB 库名保持旧值，以兼容既有数据。
- 分发产物更名为 `AzusaAI-WebUI-{full,normal,minimal}.html`；仓库纳入 git，采用 GPL-3.0 许可证，`vendor/` 与产物不进版本库。
- 默认 Base URL 改为 `http://127.0.0.1:8080/v1`；已保存的设置原样保留，不自动改写。

### 修复

- 新建会话空白页（同名桩函数覆盖了真实现）；生成中切走会话时收尾写错会话、目标消失时静默丢答。
- 存储迁移数据安全：加存储区必须升版本号并按 `objectStoreNames` 补齐，确认会话区就绪后才搬数据。
- 「清空全部 → 刷新 → 首发」后新建会话不再被下一次合并不见；动态弹窗文本框修深色系统偏好下的「深底深字」。
- 附件元数据在发请求前整体剥掉，避免严格实现因未知字段报 400；未拉取 LFS 对象时构建明确中止，不产出坏产物。
