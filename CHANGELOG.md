# 更新日志

本项目所有值得记录的变更都写在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.1] - 2026-09-15

### 新增

- **上下文管理（压缩）**：接近模型窗口上限时向模型注入用量提醒（`used/win` 与阈值来自同一口径），并支持三种压缩入口——`Compact` 工具（模型主动调用，默认权限「询问」）、应用内斜杠命令 `/compact [摘要侧重]`、设置面板与上下文面板的「立即压缩」。压缩会先生成一份交接总结，再**彻底删除**较早的消息与其附件（保留最早的若干条用户输入 + 最近的消息 + 摘要；工作区文件、后台任务、系统提示词不受影响，删除不可回看）。
- **自动压缩**：发送前与轮次之间按阈值（默认 85%，可在 设置 → MCP 工具 调 60–95 或关闭）自动压缩一次；同一水位不重复触发，单轮最多一次，失败/取消零改动；被服务端以「上下文过长」拒绝时自动压缩并重跑该轮（每轮一次）。
- **未保留消息与附件的即时释放**：被丢弃消息的附件在核对全库共享引用后立即从 IndexedDB 删除（不依赖后续 GC）；先移除消息再释放，保留集（最早的用户输入）里的附件不会被误删。
- **四个斜杠命令**：输入 `/` 开浮窗，`/compact`、`/clear`、`/help`、`/tasks`；浮窗支持前缀/子串过滤、`↑`/`↓`/`Tab` 选择、`Esc` 或点别处关闭，中文等 CJK 文字后的 `/` 不会误触发（URL 里的 `/` 同样不触发）。
- **后台任务面板 `/tasks`**：列出当前会话的后台 Python 任务与实时输出（1 秒刷新、与任务卡片同一套行渲染），可单个停止或「全部停止」（二者语义一致：销毁解释器）。
- **关于弹窗重构**：顶部 GitHub 图标 + 作者 + 版本（版本号唯一来源 `APP_VERSION`），七张使用教程卡（快速开始 / 分支 / 附件 / 工具与工作区 / 上下文与压缩 / 数据与备份 / 隐私与离线）、斜杠命令表与完整快捷键表；`Ctrl+O` 展开或折叠压缩总结气泡。
- **`AskUser` 提问工具**：模型可在对话中向你提问（每题 2–4 个选项，支持单选、多选与「其他」自由输入），你可以跳过或随时中止；作答后答案以结构化结果回传模型继续执行，跳过与中止都有明确回执。
- **工作区文件预览**：右栏点击文件即可就地预览——图片 / 文本（自动识别 GB18030、UTF-16 等编码并在弹窗里注明）/ PDF / 办公文档（docx / xlsx / pptx，图片不丢）；二进制文件给出类型与「可下载后用本机程序打开」提示，不打开弹窗。
- **目录内联树与「上一级」行**：目录可就地展开子层、逐层浏览（大目录按「显示更多」分页），列表首行可一键返回上一级。
- **上下文长度手动调节**：模型服务未返回上下文窗口时，可在生成参数浮层（「调节」）与 设置 → 生成参数 手动设置上下文长度（滑块 8K–1M、步进 8K，也可直接输入 K 值）；服务端返回窗口时该项不显示、一律以服务端为准（默认 256K 保持不变）。
- **`Read` 支持图片**：`Read("/img1.png")` 直接把图片交给支持视觉的模型（Anthropic 走 `tool_result` 内容块；OpenAI 兼容端走「工具文本 + 同一工具轮末尾一条合成 user 消息」，服务端以结构类 4xx 拒绝时自动去掉图片重试一次并提示复位方式）；模型不支持图像输入时**自动改用本机 OCR 只回文本**（对模型无感，状态行如实标注 OCR 与置信度）。预算对齐 Kimi ReadMedia：最长边 ≤2000 px、尽力压到 ≤256 KiB、单张硬顶 = 单次总量 1 MiB、单次最多 4 张；另有像素（4000 万）与字节（32 MiB）两道解码护栏与「可用 ExecutePython/Pillow 转换」的逃生舱。读一张图约 1000 token 起，工具说明里已写明。
- **图文按序融合（非视觉模型读文档）**：模型不支持图像输入时，PDF 与办公文档（docx / xlsx / pptx）里的图片不再被丢弃或跳过——本机逐张 OCR 后**按文档顺序插进正文**（PDF 按页插 `【第 N 页 · 图 k · 本机 OCR】`，办公文档按序接 `【图 k · 本机 OCR】`），`Read` 工具与「选文件发消息」两条路径同一口径。PDF 先按页内图片位置精确取区、失败自动退整页渲染（不静默，注记写明「已改用整页图像 OCR」）；预算为单次最多 6 张、总时长 ≤60 秒（附件侧整次解析共享，Read 侧 12 张 / 120 秒），达到任一预算立即停并写明「另有 N 图未处理」；OCR 引擎不可用 / 超时 / 未识别各有可读文案，**不伪造文本**。视觉模型路径与无图文档的输出一字不变；附件卡片显示「N 图已 OCR」，OCR 引擎未就绪时在环境面板「诊断」的 PDF 引擎行里如实标注（新增「图文融合」字段，与「取图」能力分开报）。
- **`Read` 支持办公文档与 PDF**：doc / docx / ppt / pptx / xls / xlsx 与 PDF 按与文本读完全相同的行号/分页契约返回，状态行给出页数 / 图数 / 是否截断；文档里的图片在支持视觉时随结果附带（单次 ≤4 张），扫描件 PDF（无文字层）自动改附前 4 页图片，非视觉模型逐页 OCR（整次 Read 共享一个 OCR 总预算，页间响应停止）。
- **工作区路径口径提示（两处定向纠错）**：`【工作区】` 上下文块、6 个文件工具与 Python / JS 两个执行工具的说明统一写明「工作区工具用 `/…`；Python 解释器里同一份数据挂载在 `/workspace`；两者指向同一份文件」；写错方向时给出可读纠错——工作区工具读 `/workspace/a.md` 会提示「你要的可能是 /a.md」；Python 里 `open("/a.txt")` 且在 `/workspace/a.txt` 真的存在时，**异常类型仍是 `FileNotFoundError`**、末尾补一句「这个文件在工作区里是 "/workspace/a.txt"」（覆盖 `builtins.open`，`pandas.read_csv` / `PIL.Image.open` 等最终走它的路径在内；`os.open` / `io.open` 不在覆盖内）。挂载工作区后即生效，不依赖会话里有没有超出预载上限的文件。
- **工具卡片图片缩略图**：`Read` 读到的图片在工具卡片展开后显示缩略图（点击放大看原图，折叠态只显示「N 张图片(展开查看)」，不加载图片数据）。
- **「优化存储」回收工具读取的图片**：工具读取的图片内联在消息里，现在同样纳入清理范围（保护最近 N 个对话的口径不变），提示区分「其中工具读取的图片 N 张」。

- **CORS 失败分层诊断 + 「预检规避兼容模式」（默认关闭）**：诊断面板新增「CORS 分层」段 —— 四条探针（简单 GET / 带应用头 GET / 简单 POST / 真实 POST）给出矩阵，并按族报出「预检被拒（OPTIONS 未过）/ 实际响应无 ACAO / 网络不可达 / 服务端未在超时内回话 / 服务端不解析 `text/plain`」，附服务端要补的响应头清单（含 `file://` 下 `Origin` 为 `null`、**错误响应也要带 `Access-Control-Allow-Origin`**：nginx 用 `add_header … always;`）；族级文案（补头 / 同源反向代理 / 关掉兼容模式）只在两处露出——诊断面板的结论行与「预检规避兼容模式」开关被拒时的红字 note（普通发送失败的文案不含族级判定：`friendlyError` / `connectHint` 的族参数分支在产线不可达）。设置 → 模型 → 高级(兼容性) 新增「预检规避兼容模式」：请求改发简单请求（`Content-Type: text/plain;charset=UTF-8`、不带任何自定义头）以绕过 OPTIONS 预检，**仅「OpenAI 兼容 + 未填 API Key」时可用**（Anthropic 与已填 Key 时置灰并写明理由），开启前自动跑四探针、**只有「简单 POST 能读到响应」才允许打开**（否则拒绝并说明「此模式无效」）；关闭时请求头 / 体逐字节不变。

### 变更

- 数据面板的 80 条裁剪改名「精简历史（只留最近 80 条）」，与「压缩上下文」的语义区分开（前者是空间回收，后者是上下文管理）。
- 版本号口径统一为用户可见 `0.1.0`（`APP_VERSION` 由历史遗留值 `2.1` 更正；环境面板、环境信息、导出备份、MCP `clientInfo`、关于弹窗与 `<meta name="version">` 全部同源）。
- 版本号升级为 `0.1.1`（`APP_VERSION` 单一来源，环境面板、环境信息、导出备份、MCP `clientInfo`、关于弹窗与 `<meta name="version">` 同步更新）。
- `/clear` 复用既有「清空消息」入口：补上忙碌门（生成中 / 压缩中拒绝）与附件即时释放，确认弹窗写明「后台任务继续运行、工作区文件不受影响」。
- **摘要输入改为完整历史**（对齐 Kimi）：压缩请求携带会话的**全部消息**（角色 / 工具调用 / 工具结果原样，图片默认原样进请求），与正常轮同一套 system / 工具集 / 采样组装（`buildBody` 单一来源）；发送前不做任何单条或总长裁剪——此前的「投影截断 + 中部省略」会让摘要看不到中段历史、压缩结果没有意义，已整体退役。
- **摘要输出预算与重试链对齐 Kimi**：预算 = `min(窗口, 128K)`（此前固定 8192），放不下时先无损收窄输出预算、不得已才缩输入；溢出按 `[0.7, 0.5, 0.35]` 缩窗（≤3 次，尾部装满、丢前导工具结果）；输出截断 / 空响应改为丢最旧一条 + 前导工具结果后重试；可重试错误指数退避（500 ms→32 s，总尝试 ≤5）；摘要请求超时 180 s → 15 分钟（状态行显示「第 N 次尝试 / 已用 M 秒」）。
- **压缩可随时取消**：`Ctrl+X` 在生成中与手动压缩（含退避等待）期间均立即中止本次压缩，取消后会话零改动；手动取消不会连带杀沙箱 / 拒绝工具等待。压缩失败提示细分为十类（上下文超窗 / 请求体积过大 / 图片格式 / 输出截断 / 内容过滤 / 空响应 / 超时 / 暂态服务端错误 / 致命错误 / 已取消），失败文案带具体原因。
- 摘要卡在「缩窗输入 / 多轮尝试」时如实标注（`摘要输入曾缩窗（丢 N 条）`、`尝试 N 次`）；`/compact` 完成提示与 Compact 工具回执同步说明摘要覆盖范围不完整。
- 「压缩上下文」「清除本会话上下文」「后台任务」进入命令面板（`Ctrl+K`）；「快捷键与关于」改名「使用教程与关于」（弹窗内容同步重构）。
- 执行器族（`ExecutePython` / `ExecuteJavaScript` / `TaskList` / `TaskOutput` / `TaskStop` / `WaitFor`）的结果统一**保留末尾**并注明截断量；档位提示、工作区回写、并发冲突、装载摘要等短注记改放**不参与裁剪的注记区**，再长的结果也不会把它们挤掉；traceback 同样改留尾。非执行器族（`Read` / `Grep` / `Glob` / `PythonPackages` / 工作区工具 / 外部 MCP）保持原有行为。
- `ExecutePython` 去掉 `packages` 参数：本档**全部预置包在首次执行前自动装载到位**（`import` 即用，无需声明任何参数；历史会话里残留该字段静默忽略）。装载时间**不计入脚本超时**（前台与后台各自两段计时），装载期间界面显示进度 `i/N`，模型侧只看到一行摘要。单个包装载失败不致命：如实列出失败包名，其余包照常可用，失败项下次执行自动补齐。
- 解释器空闲回收阈值 5 分钟 → **10 分钟**（回收时连同已装载的包一起释放；有未完成任务时保活）。
- 紧凑模式全面优化：界面密度令牌化，启用紧凑后顶栏、行距、头像、输入区等一并收紧并关闭毛玻璃；三栏共存的窗口阈值下探——1920×150% 缩放等更窄的窗口也能用（放不下时右栏自动变为浮窗）。
- 「申请持久化存储」提示改为信息级说明：Chrome 106 起该权限不再弹授权框、由浏览器静默决策，「未授予」是正常现象；已授予时按钮显示「已授予持久化存储」并禁用，环境面板补充成因说明。
- `AskUser` 提问：**单题且单选时，点选项即自动提交**（走与「提交回答」完全相同的提交路径与守卫；多选 / 多题 / 「其他」自定义输入的行为不变）。
- `Read` 的文本读渲染内核抽成共享分页助手（`wsLinePageResult`）供图片 / 文档 / OCR 结果复用：文本读的输出**逐字节不变**（9 组 fixture × 5 组参数在批前产物上对跑核验），新增的文档与 OCR 结果与文本读共用同一套行号 / 翻页 / 截断坐标契约。
- 工具读取的图片在请求装配层加了两条纪律：合成 user 消息只存在于请求体（不落盘、不进界面）；较久的工具图片超过「最近 8 条 / 8 MiB」不再随请求重发（存储不动、只影响本次请求），工具图片也计入上下文用量估算。
- **生成中的待发送缓冲区（对齐 Kimi 的排队机制）**：模型在 Agent 循环里工作时，输入框回车不再被静默吞掉 —— 文本进缓冲区，输入框上方浮窗逐条列出（单行截断 + `title` 全文）；**每条在下一个工具轮边界插入会话**（工具结果落地、下一次模型请求前即作为 user 消息补进去，模型当轮就能看到，支持中途补充需求），最后一轮之后到达的在整轮结束时兜底发送（FIFO）；循环结束前可点行内「编辑」或按 `↑`（输入框为空时）把内容召回重编、或点「丢弃」删除。缓冲按**入队时会话**归属：生成期间切走会话浮窗隐藏、回来可见，消息仍落回原会话；目标会话已被删除则整槽丢弃并提示。`Ctrl+X` 停止 / 请求失败后照发（对齐 Kimi）；普通 `Esc` 丢弃全部待发送（浮窗 hint 明示「Esc 丢弃待发送」）；带附件或办公文档仍在解析的消息 v1 不入队并给出提示；条目不持久化（刷新即失），上下文用量环把缓冲文本一并计入「待发送草稿」。会话切换 / 删除 / 新建 / 整库替换都会重算浮窗可见性（挂在 `renderChat` 的两个出口）。空缓冲时该链路是纯 no-op（以「无缓冲一轮 = 恰 1 个请求 + 浮窗恒隐藏」的基线断言核过）。
- **中断键改为 `Ctrl+X`（macOS `Cmd+X`）**：停止生成 / 中止手动压缩统一改由 `Ctrl+X` 触发 —— 避免与 Windows 系统快捷键冲突（`Ctrl+Esc` 开始菜单 / `Ctrl+Shift+Esc` 任务管理器 / `Ctrl+Alt+Esc` 部分机器被占用）；有选中文本时 `Ctrl+X` 仍是剪切，不拦截。普通 `Esc` 不再中断，只负责关浮层 / 弹层，并在其后丢弃待发送缓冲区（无缓冲则不再有其它动作）。停止按钮悬停提示、命令面板与使用教程快捷键表已同步。
- **模型侧提示词统一对齐 Kimi 风格规范**：15 个工具描述、4 个系统注入块与工具结果注记按统一骨架重写（首行边界句 + 空行 + `- ` 列表要点；`ExecutePython` 分「两种用法 / 环境与探索 / 边界 / 输出与截断 / 后台任务」五节），格式骨架 / 分节 / 标点（半角括号、反引号标识符）统一，参数事实的唯一权威归 `inputSchema` 描述、`next_step:` 只加在含出路的句子上，双面文案去用户祈使、去处改事实括注；重写以「信息零丢失」为硬门（与改写前逐句差分核过），`Read` / `Write` 等描述结构重排但要点一条未减。

### 修复

- 工具结果的「完整输出尾部可用 `TaskOutput` 取」提示不再无条件附加：只有**真的被截断**时才出现，未截断时结果里一个多余字符都不加。
- 后台任务之后的续思写进**它自己的**思考块（此前会落到同一气泡里上一轮的气泡中），删除路径同样按消息身份归属。
- 任务卡片日志容器可真实拖拽 / 滚轮翻阅：日志行不再每秒被整棵重建（此前拖动会被打断、`scrollTop` 归零），窗口里保留末 8 KB 输出，非贴底时新输出不夺回滚动位置，整行重建后仍停在最新几行。
- `PythonPackages` 去掉 ② 段 80 条硬上限：默认一次给全，并新增 `offset` / `limit` 分页（尾部给出下一页 offset，`offset` 越界给可读回复）。
- JS 沙箱的 `console` 日志改为「丢最早 + 计数 + 单条留尾」，并在结果里注明丢弃条数（此前满 120 条后静默丢头）。
- `TaskOutput` 快照的输出尾部截断（8 KB）补上注记（此前静默）。
- **工作区文件工具的编码与二进制处理整体重做**：`Read` 不再把中文等合法文本误判成二进制、返回 base64——按 UTF-8 / UTF-16 / GB18030 自动识别、按行读出并注明编码；二进制（图片 / 压缩包 / 办公文档 / PDF / 可执行文件等）一律拒绝并报出类型与替代路径（错误码 `EBINARY`，需要字节请用 `ExecutePython` 以 `rb` 打开）；`Edit` 拒绝非 UTF-8 与含 NUL 的文件（此前会写坏非 UTF-8 字节），纯 CRLF 文件读写往返不再必然失配；`Grep` 不再跳过中文文件、`path` 可指向单个文件；`Write` 拒写二进制后缀；`Glob` 新增 `head_limit`（旧名 `limit` 仍可用）；超长单行可用新增的 `column_offset` 续读。
- **附件编码修复**：GBK / GB18030 等非 UTF-8 文本附件按真实编码读取，并在附件卡与发给模型的前缀里注明「编码: GB18030(已转码显示)」（此前静默乱码）；真二进制改名 `.txt` 仍被拒收并报出类型。
- 非流式响应里同一条消息的**多个工具调用不再被合并**成一个（此前工具名会被拼接、调用必然失败；流式路径与 Anthropic 协议不受影响）。
- 启动后的第一笔保存不再用数据库副本覆盖窗口期内的内存改动（此前刷新后的状态收敛可能被静默替换丢失）。
- 工作区 PDF 预览不再显示「已抽取文字发给模型」（本地预览不发送任何数据，此前是错误陈述）。
- 弹窗按钮宽度统一（1:1）：删除确认、右栏底部、提问对话框等此前宽度参差的按钮，现统一撑满等宽且不换行。
- 外部 MCP 工具列表改为限高滚动（长列表不再把面板撑长），instructions 与警告行固定在其上方；没有外部工具时不再显示空框。
- 右栏「窄窗口打不开、缩窄收起后拉宽不恢复」修复：窗口放不下时自动变为浮窗（≤880px 为抽屉 + 遮罩），缩窄自动收回、拉宽自动恢复，手动开合与自动让位互不干扰。
- **`AskUser` 提问卡片五项体验修复**：卡片出现时对话自动滚到底（用户正在上翻时不抢占）；多题改为「一屏一题」`←/→` 轮播、卡片高度恒为单题且切题不丢草稿；「提交回答」移到底部右侧（与「跳过」同行）；提交或跳过后卡片自动收起（状态行与摘要保留，刷新后同样收起）；单选且非最后一题时点选即自动跳下一题（多选、末题、「其他」输入与取消点选均不跳）。
- **右栏拖拽上传修复**：拖入文件不再误报「没有可上传的文件」（可读取的来源正常识别并上传）；浏览器拿不到文件内容的受限来源，改为给出可操作提示，不再只报失败。
- 状态栏「估算的下一轮用量」补齐两处遗漏：此前只复制「工作区 + 外部 MCP」两块系统注入，现与请求装配同源同序补上「后台任务完成提醒」与「上下文用量提醒」（用只读变体，不登记在途通知）——有「待告知后台任务」或已过提醒水位时估算不再偏低。
- 「图文融合」状态行改三态：图像能力未知（`auto` 且尚未探测到该模型；探针受阻、拿不到结论时同样归此态，不写「不支持图像输入」）不再被写成「支持图像输入」，如实标注「图像能力未知」，并给出「把该模型图像能力设为不支持即可启用 OCR 融合」的入口。

### 已知影响

- **自动压缩会产生一次额外模型请求**（摘要请求）并且是**不可逆的消息删除**：可在 设置 → MCP 工具 关闭自动压缩或调阈值；手动压缩必带确认弹窗与「先导出备份」提示。
- 摘要请求的输出预算 = `min(模型窗口, 128K)`（此前固定 8192），**不受会话的 `max_tokens` 设置影响**；摘要请求自身的 token 消耗不计入会话的用量环与累计统计。摘要输入 = 全量历史（图片默认按原样进入请求，超体积时自动降级省略较早的图片），长会话首次尝试可能需要数分钟（最坏 ≈ 单次 15 分钟 × ≤5 次；状态行显示已用时长，按 `Ctrl+X` 可随时取消，取消后会话零改动）。
- `notified` 语义微调：任务结束时若所属会话已无消息（例如刚 `/clear`），不再向该会话注入「任务完成」提醒（也不会因此自动续跑）——排查时 `notified=true` 不再等价于「模型收到过」，要看是否有对应请求。
- **首次执行明显变长**：会先装载本档全部预置包（完整版 151 / 正常版 99 / 极简版 33），界面有进度、不计入超时；同一解释器生命期内只装一次，之后每次执行只做一次廉价比对。这是有意的取舍（宁可等，也要"第一次运行即成功"）。
- **工具 schema 变化会影响远端缓存**：`tools[]` 变化（`ExecutePython` 少了 `packages`；`Read` 去掉 `byte_offset`、新增 `column_offset`；`Glob` 新增 `head_limit`）⇒ 远端需要为相关对话重新 prefill 一次（首字变慢，历史内容不受影响），协议层无法规避。

## [0.1.0] - 2026-09-14

首个公开版本：单文件、离线、零外部请求的对话终端，内置 Python 运行时与模型侧工具。

### 新增 — 对话与生成

- OpenAI 兼容（`/v1/chat/completions`）与 Anthropic（`/v1/messages`）双协议，一键切换。
- 流式逐字输出；浏览器不支持流式读取时自动降级为非流式。
- 用户与 AI 消息都渲染 Markdown（表格 / 任务列表 / 引用 / 围栏代码），思考内容同样渲染。
- 思考过程（`reasoning_content` / Anthropic `thinking`）渲染为可折叠块并显示耗时。
- 内嵌 KaTeX 数学公式：公式先抽占位符再回填，LaTeX 反斜杠不会被 Markdown 吃掉。
- 代码块表头显示语言与行数；超 10 行块内滚动、可展开、可复制；HTML / SVG 可一键运行。
- 重新生成走分支：不删后续消息，`‹ n/N ›` 切换旧结果。
- 编辑任意用户消息并重新生成，自动截断其后的消息。
- 上下文用量环：实时显示 `16.4K/256.0K`，点击展开本次请求构成与本对话累计明细。
- 生成统计：token 优先取服务端 `usage`，速度取客户端实测吞吐（首字 → 最后一帧）；拿不到时按四桶字符模型估算并加 `≈`。
- 会话管理：新建 / 重命名 / 置顶 / 副本 / 删除 / 按时间分组 / 标题与全文搜索 / 会话内查找（`Ctrl+F`）/ 命令面板（`Ctrl+K`）。

### 新增 — 知识与文件

- 附件体系：纯文本 / 图片 / PDF / 办公文档；单文件 ≤ 50MB、一条消息 ≤ 10 个、单条消息总量默认 ≤ 100MB（可调）。
- 类型把关：二进制文件明确拒收并报出类型；宏格式提示另存为无宏；无后缀文件先读前 64KB 嗅探。
- 图片附件支持 PNG / JPG / GIF / WebP / BMP / AVIF / SVG / ICO / TIFF，统一重编码并以 data URL 内联发送；超「图片最大边长」（默认 4096px）自动下采样。
- PDF 双模式：纯文本抽取（插入页分隔）或逐页渲染成图片（72–300 DPI，页数上限可设）。
- 图片 OCR 回退：模型不支持视觉时用内嵌 Tesseract.js（中 / 英 / 中英可选）只发识别文字。
- 图像输入能力探测：服务端清单声明 + 1×1 图片探测，可在设置里手动覆盖。
- 办公文档六格式本机解析（`doc` `docx` `ppt` `pptx` `xls` `xlsx`）：docx 抽正文 + 图片、xlsx 转 CSV、老格式仅文本；worker 内零网络。
- 附件卡片与预览弹窗（PDF 图片模式给每页缩略图）；图片灯箱支持滚轮缩放 / 拖拽平移 / 双击复位。
- 语音输入（静音 5 秒自动收尾）与语音合成朗读。

### 新增 — 代码执行

- 内嵌 pyodide 314.0.6（CPython 3.14.2）核心集 + 预置包 wheel：离线可跑 Python，无「首次下载 / 首次安装」环节。
- 三档产物：完整版 151 / 正常版 99 / 极简版 33 个 wheel；档位名单唯一权威 = `scripts/pyodide-profiles.json`（构建期断言防漂移）。
- `ExecutePython`：`code` 或工作区 `script` 二选一，支持 `args` / `stdin` / `cwd` / `files` / `packages` / 超时；stdout / stderr / 退出码一并回传。
- Python 任务管理：前台 / 后台执行 + `TaskList` / `TaskOutput` / `WaitFor` / `TaskStop`；并发上限 2 个未完成任务；任务完成自动回到对话。
- `PythonPackages`：查询「已加载 / 已预置 / 不可用」三段包清单，可按发行名或 import 名过滤。
- 会话工作区（沙箱虚拟盘）：6 个文件工具（`Read` / `Write` / `Edit` / `Glob` / `Grep` / `FileOps`）、上传 / 下载 / 整箱 ZIP、挂载与持久化存储申请。

### 新增 — 工具

- 模型侧 MCP 工具框架：注册表 + 权限三态（允许 / 询问 / 禁止）+ 限额（超时 / 每轮次数 / 结果长度）。
- 内置 `ExecuteJavaScript` 沙箱：独立 Web Worker，无 DOM、无页面数据、默认无网络。
- 外部 MCP 客户端：可连接外部 MCP 服务器，全局开关 + 会话级覆盖。
- 工具调用与正文合并渲染在同一气泡；调用过程渲染为可展开的工具卡片（参数与返回值）。
- 面板级「允许工具联网」开关；内存审计日志保留最近 100 条。

### 新增 — 外观与数据

- 12 种马卡龙主题色（默认樱花粉）+ 深色 / 浅色 / 跟随系统 + 紧凑模式。
- 设置面板 7 个标签页（模型 / 生成 / 附件 / 外观 / 数据 / MCP 工具 / 环境）与环境自检面板（可一键复制环境信息）。
- 会话正文与附件存入 IndexedDB（v3 五个存储区）；localStorage 只放设置与轻量状态。
- 打开时自动整理（旧库搬进大仓库 + 回收无引用附件），无损、无感、不做无感删除。
- 「清理空间」弹窗：勾选 + 确认才动数据，带保护范围滑块（默认最近 30 个对话不动）。
- 导出 / 导入备份（附件内联，可完整恢复）；导出单个会话为 Markdown。
- 多标签页同步；提示词模板库。

### 变更

- 应用更名为 AzusaAI WebUI；存储键（`chatgpt-webui-v1`）与 IndexedDB 库名保持旧值，以兼容既有数据。
- 分发产物更名为 `AzusaAI-WebUI-{full,normal,minimal}.html` 并落在代码目录根；早期产物名为 `index.html`（已弃用），两份旧产物落点均已由 `.gitignore` 设防。
- 目录结构规范化为 `src/`（源码）/ `scripts/`（构建与工具链）/ `docs/`（文档）/ `vendor/`（上游发行版）。
- 仓库纳入 git；采用 GPL-3.0 许可证；新增 `THIRD_PARTY_NOTICES.md`；`vendor/` 与三份产物不进版本库。
- 构建链迁移为纯 `node`：`build.js` / `syntax.js` / `lint.js` / `make-*-part.js`，无 npm 依赖。
- `build.js --profile=all` 采用原子写出：三档全部成功才 rename 到正式名，失败不留半套产物。
- 项目不长期保留回归 / 测试脚本；验证脚本按需现写现删。
- 默认 Base URL 改为中性本机回环地址（`http://127.0.0.1:8080/v1`）：公开源码不再携带任何内网地址；已保存的设置一律原样保留，不做自动改写。

### 修复

- 新建会话空白页：删除会覆盖真实现的同名桩函数，空会话统一渲染欢迎页。
- 存储迁移数据安全：IndexedDB 加存储区必须升版本号、按 `objectStoreNames` 补齐，并确认会话区就绪后才允许把会话从 localStorage 搬走。
- 生成中切走会话：收尾与工具轮按「发起生成时的会话 id」写回；目标消失时不发请求并如实提示，不再静默丢答。
- 「清空全部 → 刷新 → 首发」：整库替换标记的消费时机修正，避免新建会话被下一次合并不见。
- 两级确认弹窗吞掉 Enter 自动重复，避免按住一次按键连过两级确认。
- 动态弹窗的文本输入显式写 `type="text"`，修复深色系统偏好下「深底深字」。
- 附件元数据（`attach`）在发请求前整体剥掉，避免严格实现因未知字段报 400。
- 非流式路径缺陷修复与 favicon MIME 修正。
- LFS 指针守卫：克隆端未拉取 LFS 对象时构建明确中止，不产出「构建成功但 Python 工具全坏」的静默坏产物。
